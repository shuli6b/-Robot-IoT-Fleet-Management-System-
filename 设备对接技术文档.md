# 机器人物联网管理系统 — 设备对接技术文档与通信协议规范

> **合同编号**：IOT-20260811  
> **文档密级**：商用交付物  
> **适用对象**：硬件集成工程师、现场自动化工程师、机器人二次开发人员  
> **文档版本**：v1.0（2026-08-15）

---

## 一、MQTT 通信基础规范

系统采用标准 MQTT v3.1.1 / v5.0 物联网传输协议，基于 TCP 长连接实现双向稳定通信。

### 1.1 连接参数配置

| 参数名称 | 推荐配置值 | 说明 |
|---|---|---|
| **协议类型** | `MQTT` (TCP) | 现场标准工业物联网协议 |
| **Broker 默认端口** | `1883` | 未加密标准 TCP 端口（生产环境可按需启用 8883 MQTTS） |
| **心跳保持 (Keep Alive)**| `60` 秒 | 设备端建议设置 30~60 秒，服务端超时 30 秒无心跳自动判定离线 |
| **发布服务质量 (QoS)** | `QoS 1` (At least once) | 保证重要工业数据与告警报文至少送达一次 |
| **Clean Session** | `True` | 每次重连后刷新会话，避免旧离线消息积压 |
| **认证方式** | 用户名 + 密码认证 | 杜绝匿名连接，详见下表 |

### 1.2 预分配设备接入账号

| 设备品类 | 接入用户名 (Username) | 初始密码 (Password) | 发布权限范围 (Topic ACL) |
|---|---|---|---|
| **华数机械臂** | `device_huashu_arm` | `Arm_Secure_2026!` | `robot/huashu_arm/#` |
| **珞石 AMR** | `device_luxshare_amr` | `Amr_Secure_2026!` | `robot/luxshare_amr/#` |
| **四足机器狗** | `device_robot_dog` | `Dog_Secure_2026!` | `robot/robot_dog/#` |

---

## 二、Topic 命名规则与层级规范

系统采用四级分层 Topic 规范：

```
robot/{device_type}/{device_id}/{data_type}
```

```
┌───────┐     ┌──────────────┐     ┌─────────────┐     ┌─────────────┐
│ robot │  /  │{device_type} │  /  │ {device_id} │  /  │ {data_type} │
└───────┘     └──────────────┘     └─────────────┘     └─────────────┘
  前缀            设备品类              设备唯一ID           数据类型
固定为 robot   如: huashu_arm        如: arm_001        state / sensor
```

### 2.1 字段定义与示例

1. **`robot`**（第 1 级）：固定前缀，用于系统全局通配订阅 `robot/#`。
2. **`{device_type}`**（第 2 级）：设备品类，小写下划线命名：
   - `huashu_arm`：华数系列六轴/四轴工业机械臂
   - `luxshare_amr`：珞石 AMR 自主移动搬运机器人
   - `robot_dog`：四足巡检机器狗
   - *（后续新增其他品类设备可直接拓展，无需修改后台代码）*
3. **`{device_id}`**（第 3 级）：同类型内设备唯一出厂/配置标识，如 `arm_001`、`amr_002`、`dog_001`。
4. **`{data_type}`**（第 4 级）：数据类型枚举：
   - `state`：核心运行状态与工况报文（建议 1~5 秒上报一次）
   - `sensor`：传感器环境与电气遥测报文（建议 3~10 秒上报一次）
   - `alarm`：突发事件与故障告警报文（事件触发式上报）

---

## 三、JSON 报文格式与字段释义

所有上报 Payload 均必须为标准 UTF-8 编码的 JSON 字符串。

### 3.1 状态数据 (`state`)

**Topic 示例**：`robot/huashu_arm/arm_001/state`

```json
{
  "timestamp": "2026-08-15T10:30:00",
  "status": "running",
  "battery": 85,
  "error_code": 0,
  "error_msg": "",
  "joint_angles": [0.5, 12.3, -45.0, 90.0, -15.2, 0.0],
  "speed": 60,
  "payload_kg": 5.2,
  "cycle_count": 1024
}
```

#### 字段释义表

| 字段名 | 类型 | 必填 | 说明与取值范围 |
|---|---|---|---|
| `timestamp` | string | 是 | 设备当前时间戳（ISO 8601 格式，如 `2026-08-15T10:30:00`） |
| `status` | string | 是 | 运行工况枚举：`running`(运行中) / `idle`(待机) / `charging`(充电中) / `navigating`(导航行进) / `patrolling`(巡检中) / `standing`(站立待命) / `sitting`(蹲伏) / `stopped`(停止) / `error`(故障) |
| `battery` | int/float | 是 | 剩余电量百分比（`0` ~ `100`） |
| `error_code` | int | 是 | 故障码，`0` 表示完全正常，非 0 表示对应故障代码 |
| `error_msg` | string | 否 | 故障描述文本（如为空可传 `""`） |
| `model` | string | 否 | 官方型号（如 `华数BR610工业机械臂`、`珞石SR3+定制AGV`、`南沙四足机器狗`） |
| `running_hours`| float | 否 | 设备累计运行工作小时数（用于健康管理与保养周期推算） |
| `di_status` | array | 否 | 数字量输入信号矩阵 `[DI1, DI2, DI3, DI4]`（`1` 闭合 / `0` 断开） |
| `do_status` | array | 否 | 数字量输出执行矩阵 `[DO1, DO2]`（`1` 激活 / `0` 关闭） |
| `joint_angles`| array | 否 | 机械臂专有：六轴关节当前物理角度数组 `[J1, J2, J3, J4, J5, J6]`（单位：度） |
| `speed` | int | 否 | 机械臂专有：全局运行速率百分比（`0` ~ `100`%） |
| `payload_kg` | float | 否 | 机械臂专有：末端夹爪当前负载重量（单位：kg） |
| `cycle_count`| int | 否 | 机械臂专有：已完成的流水线工件节拍总计数 |
| `position` | object | 否 | AMR 专有：空间位置坐标 `{"x": 12.5, "y": 8.3, "floor": 1}`（单位：米） |
| `arm_sr3_pose`| array | 否 | 珞石 SR3 复合臂位姿数组 `[J1, J2, J3, J4, J5, J6]` |
| `speed_mps` | float | 否 | AMR/机器狗专有：当前行进线速度（单位：m/s） |
| `map_id` | string | 否 | AMR 专有：当前运行的高精地图 ID |
| `current_task`| string| 否 | AMR 专有：当前执行的调度任务号（如 `pick_and_place` / `floor_patrol`） |
| `load_status`| string | 否 | AMR 专有：载重状态（`loaded` 载货 / `empty` 空载） |
| `gait_mode` | string | 否 | 机器狗专有：步态类型（`trot` 小跑 / `walk` 漫步 / `bound` 跃动） |
| `imu_pitch` | float | 否 | 机器狗专有：IMU 俯仰姿态角（单位：度） |
| `imu_roll` | float | 否 | 机器狗专有：IMU 翻滚姿态角（单位：度） |
| `uav_collab_ready`| bool | 否 | 机器狗专有：空地搜救无人机协同链路就绪标识 |

---

### 3.2 传感器遥测与全维度商业环境数据 (`sensor`)

**Topic 示例**：`robot/luxshare_amr/amr_001/sensor` 或 `robot/robot_dog/dog_001/sensor`

```json
{
  "timestamp": "2026-08-15T10:30:00",
  "temperature": 42.5,
  "humidity": 55.0,
  "vibration": 0.025,
  "voltage": 24.2,
  "current": 3.8,
  "co2_ppm": 520,
  "pm25": 18,
  "hcho": 0.015,
  "voc": 0.10,
  "noise_db": 53.5,
  "human_presence": true,
  "foot_traffic": 142
}
```

#### 字段释义表

| 字段名 | 类型 | 必填 | 单位 | 说明 |
|---|---|---|---|---|
| `timestamp` | string | 是 | - | 数据采集时刻（ISO 8601） |
| `temperature` | float | 是 | °C | 核心电机/本体工作温度 |
| `humidity` | float | 否 | % | 现场环境相对湿度 |
| `vibration` | float | 否 | g | 本体三轴合成振动加速度有效值 |
| `voltage` | float | 是 | V | 母线供电电压（如机械臂 24V / AMR 48V） |
| `current` | float | 是 | A | 实时总母线工作电流 |
| `co2_ppm` | int | 否 | ppm | 商业环境二氧化碳浓度（空气质量） |
| `pm25` | int | 否 | μg/m³ | 颗粒物 PM2.5 浓度 |
| `hcho` | float | 否 | mg/m³ | 室内甲醛浓度（环保健康） |
| `voc` | float | 否 | mg/m³ | 挥发性有机物浓度 |
| `noise_db` | float | 否 | dB | 现场声学环境噪声分贝 |
| `human_presence`| bool | 否 | - | 人体存在红外/雷达感知 (`true` 有人 / `false` 无人) |
| `foot_traffic` | int | 否 | 人次 | 商业区域累计客流人数统计 |

---

### 3.3 突发告警数据 (`alarm`)

**Topic 示例**：`robot/huashu_arm/arm_001/alarm`

```json
{
  "timestamp": "2026-08-15T10:30:00",
  "alarm_level": "warning",
  "alarm_code": "E102",
  "alarm_msg": "J2号关节电机温度过高 (82°C)",
  "resolved": false
}
```

---

### 3.4 下行任务控制指令 (`cmd` - 调度系统)

**Topic 格式**：`cmd/{device_type}/{device_id}`  
**示例**：`cmd/luxshare_amr/amr_001` 或 `cmd/huashu_arm/arm_001` 或 `cmd/robot_dog/dog_001`

```json
{
  "task_id": "T20260815-001",
  "command": "pick_and_place",
  "params": {
    "source_station": "ST_A01",
    "target_station": "ST_B03",
    "arm_action": "grip"
  },
  "timestamp": "2026-08-15 10:00:00"
}
```

#### 下行指令常用枚举速查

| 设备品类 | 指令名称 (`command`) | 常用参数 (`params`) | 动作说明与业务场景 |
|---|---|---|---|
| **珞石 AMR** | `pick_and_place` | `{"source_station": "ST_A01", "target_station": "ST_B03"}` | 定点取放料调度任务 |
| **珞石 AMR** | `auto_charge` | `{"charger_id": "CHG_BAY_01", "min_battery": 95}` | 低电压智能自动回充任务 |
| **珞石 AMR** | `floor_patrol` | `{"floor": 1, "route": ["P1", "P2"], "obstacle_avoid": true}` | 楼层跨区域避障巡检 |
| **珞石 AMR** | `stop` | `{"reason": "manual_estop"}` | 紧急制动停车 |
| **华数 BR610** | `start_cycle` | `{"speed": 80, "cycle_limit": 500, "program": "BR610_01"}` | 启动自动化生产加工节拍 |
| **华数 BR610** | `reset` | `{}` | 机械臂零位校准与故障复位 |
| **华数 BR610** | `pause` | `{}` | 暂停当前节拍动作 |
| **华数 BR610** | `stop` | `{"reason": "safety_door_open"}` | 安全门连锁或急停中断 |
| **四足机器狗** | `patrol` | `{"route": "LOBBY_1F", "sensors": ["co2", "foot_traffic"]}` | 商业环境与客流巡检 |
| **四足机器狗** | `uav_rescue_collab`| `{"uav_sync": true, "thermal_scan": true}` | 空地搜救无人机协同就绪 |
| **四足机器狗** | `stand` / `sit` | `{}` | 站立待命 / 蹲伏休眠模式切换 |

---

## 四、各品类设备对接示例代码

### 4.1 Python 对接示例（通用版 / 工控机适用）

```python
import time
import json
from datetime import datetime
import paho.mqtt.client as mqtt

# 1. 配置参数
MQTT_HOST = "192.168.1.100"  # 部署服务器 IP
MQTT_PORT = 1883
USERNAME = "device_huashu_arm"
PASSWORD = "Arm_Secure_2026!"

DEVICE_TYPE = "huashu_arm"
DEVICE_ID = "arm_001"

# 2. 初始化客户端
client = mqtt.Client(client_id=f"device_{DEVICE_TYPE}_{DEVICE_ID}")
client.username_pw_set(USERNAME, PASSWORD)

def on_connect(c, userdata, flags, rc):
    if rc == 0:
        print(f"[OK] 成功连接至 MQTT 服务器 {MQTT_HOST}:{MQTT_PORT}")
    else:
        print(f"[ERROR] 连接失败，返回码: {rc}")

client.on_connect = on_connect
client.connect(MQTT_HOST, MQTT_PORT, keepalive=60)
client.loop_start()

# 3. 周期性上报循环
try:
    while True:
        now_str = datetime.now().strftime("%Y-%m-%dT%H:%M:%S")
        
        # 构造并发送状态报文
        state_payload = {
            "timestamp": now_str,
            "status": "running",
            "battery": 88,
            "error_code": 0,
            "error_msg": "",
            "joint_angles": [0.0, 15.0, -30.0, 45.0, 0.0, 0.0],
            "speed": 75,
            "payload_kg": 4.5,
            "cycle_count": 520
        }
        topic_state = f"robot/{DEVICE_TYPE}/{DEVICE_ID}/state"
        client.publish(topic_state, json.dumps(state_payload), qos=1)
        
        # 构造并发送传感器报文
        sensor_payload = {
            "timestamp": now_str,
            "temperature": 41.5,
            "humidity": 52.0,
            "vibration": 0.02,
            "voltage": 24.1,
            "current": 3.2
        }
        topic_sensor = f"robot/{DEVICE_TYPE}/{DEVICE_ID}/sensor"
        client.publish(topic_sensor, json.dumps(sensor_payload), qos=1)
        
        print(f"[{now_str}] 数据上报成功 -> {DEVICE_ID}")
        time.sleep(3.0)  # 3秒上报一次

except KeyboardInterrupt:
    print("停止上报...")
finally:
    client.loop_stop()
    client.disconnect()
```

---

### 4.2 C++ 对接示例（基于 Paho MQTT C++ 库）

```cpp
#include <iostream>
#include <string>
#include <chrono>
#include <thread>
#include "mqtt/async_client.h"

const std::string SERVER_ADDRESS("tcp://192.168.1.100:1883");
const std::string CLIENT_ID("amr_001_controller");
const std::string TOPIC("robot/luxshare_amr/amr_001/state");

int main() {
    mqtt::async_client client(SERVER_ADDRESS, CLIENT_ID);
    mqtt::connect_options connOpts("device_luxshare_amr", "Amr_Secure_2026!");
    connOpts.set_keep_alive_interval(60);
    connOpts.set_clean_session(true);

    try {
        client.connect(connOpts)->wait();
        std::cout << "AMR MQTT 已连接成功!" << std::endl;

        while (true) {
            std::string payload = "{\"timestamp\":\"2026-08-15T10:30:00\","
                                  "\"status\":\"navigating\",\"battery\":82,"
                                  "\"error_code\":0,\"position\":{\"x\":10.2,\"y\":5.6,\"z\":0.0},"
                                  "\"speed_mps\":1.2,\"current_task\":\"task_001\"}";
            
            mqtt::message_ptr pubmsg = mqtt::make_message(TOPIC, payload);
            pubmsg->set_qos(1);
            client.publish(pubmsg)->wait_for(1000);
            
            std::cout << "AMR 上报成功: " << payload << std::endl;
            std::this_thread::sleep_for(std::chrono::seconds(3));
        }
    }
    catch (const mqtt::exception& exc) {
        std::cerr << "MQTT 错误: " << exc.what() << std::endl;
        return 1;
    }
    return 0;
}
```

---

## 五、系统常见故障异常排查手册

| # | 故障现象 | 可能原因 | 排查步骤与解决方案 |
|---|---|---|---|
| 1 | **设备连接 MQTT 被拒绝 (Connection Refused: Not Authorized)** | 用户名或密码错误，或 EMQX 开启了安全鉴权 | 检查客户端配置的 `username` 和 `password` 是否与 EMQX 后台配置一致，确认没有拼写错误。 |
| 2 | **数据发送成功，但 Web 界面未显示该设备** | 1. Topic 格式不符合 `robot/{type}/{id}/{datatype}`<br>2. 顶层前缀非 `robot` | 检查发送的 Topic，必须严格为 4 段（以 `robot/` 开头，如 `robot/huashu_arm/arm_001/state`）。查看 `/opt/robot-iot/robot_iot.log` 确认是否有 warning 告警。 |
| 3 | **设备上报一段时间后在 Web 界面变为【离线】(offline)** | 设备停止发送，或上报间隔超过 30 秒超时阈值 | 确认设备端程序是否仍在正常循环运行，检查网络连接是否中断。确保设备上报周期在 1~10 秒以内。 |
| 4 | **Web 界面提示「网络连接异常，正在重试」** | FastAPI 后端服务未运行或 8000 端口被防火墙拦截 | 1. 执行 `systemctl status robot-iot` 检查服务是否存活。<br>2. 执行 `sudo ufw status` 检查 8000 端口是否放行。<br>3. 检查服务器内网 IP 是否发生变动。 |
| 5 | **上报报文 JSON 解析异常 (JSON Decode Error)** | 发送的内容非标准合法 JSON（如包含单引号或编码不是 UTF-8） | 检查设备端 JSON 序列化输出，确保键名使用双引号 `""`，使用 UTF-8 字符集。后端会自动捕获并将原始数据留存，不影响服务运行。 |
| 6 | **EMQX 服务无法启动 (Port 1883 in use)** | 本机已有其他 MQTT 软件（如 mosquitto）占用了 1883 端口 | 执行 `sudo netstat -tlnp \| grep 1883` 查看占用进程，停用冲突服务后重启 EMQX：`sudo systemctl restart emqx`。 |
| 7 | **历史数据分页查询缓慢或卡顿** | 历史数据量巨大（百万级以上）未走索引 | 数据库在 `(device_id, device_type, received_at DESC)` 已建立联合索引。分页查询时务必指定目标设备与每页限制（`page_size <= 100`）。 |
| 8 | **数据库出现 database is locked 错误** | 未开启 WAL 模式或多个外部程序强行排他写入 | 本系统已默认启用 `PRAGMA journal_mode=WAL` 与 `busy_timeout=5000`。请勿使用外部 SQLite 工具在写入高峰期独占加锁打开 `robot.db`。 |
| 9 | **外网设备无法连接局域网内的后台服务器** | 路由器未做端口映射，或云服务器安全组未放行 1883 | 在外网部署时，需在路由器/防火墙将外部端口 `1883` 映射至服务器内网 IP，或在云服务器安全组添加入站放行规则。 |
| 10 | **断电重启后后台与 MQTT 未自动启动** | 未正确配置 systemd 开机自启服务 | 执行 `sudo systemctl enable emqx` 与 `sudo systemctl enable robot-iot`，确保两个服务均为 enabled 状态。 |

---
**技术支持单位**：广州擎天智技术有限责任公司  
**联系工程师**：张工 / 13940194687
