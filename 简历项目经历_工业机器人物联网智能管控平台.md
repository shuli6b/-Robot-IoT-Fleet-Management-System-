# 📄 个人简历项目经历精炼库 —— 工业级机器人物联网智能管控与数字孪生平台

> 本文档专为简历制作、面试问答与背景调查准备。已将项目中所有技术难点、架构设计、技术栈与量化成果按 HR/技术面试官青睐的 **STAR 法则** 进行提炼。

---

## 🎯 一、 简历极简版（适合直接粘贴至简历“项目经历”栏）

### **项目名称**：工业级多品类机器人物联网智能管控与数字孪生平台
**项目角色**：核心全栈开发 & 系统架构工程师  
**核心技术栈**：Python / FastAPI / EMQX (MQTT) / SQLite (WAL) / 华数机器人 SDK / FRP 穿透容灾 / LLM 大模型编译 / TailwindCSS / Systemd

- **架构与协议对接**：主导工业物联网平台架构设计，构建基于 EMQX 的 MQTT 高并发消息总线，完成华数 6 轴工业机械臂、珞石复合 AMR、四足巡检机器狗、协同无人机等 10+ 跨品类设备的实时遥测采集（1Hz 遥测/传感器双流，延迟 <20ms）。
- **底层二次开发与姿态联动**：深入对接华数 HSC3 控制柜底层通信协议，基于 C#/C++ 与 Python 实现 6 轴关节角度、末端空间笛卡尔坐标、电机负载电流等 20+ 核心工况的高保真逆解与 3D 数字孪生姿态动态联动。
- **AI 工业指令智能编译器**：集成 DeepSeek / 通义千问 LLM 接口与工业规则引擎，首创口语化指令实时编译机制，在 0.5s 内将人类自然语言精准转化为标准 MQTT 控制指令与 JSON 参数并闭环执行。
- **高可用内网穿透与双机容灾**：基于 FRP 设计安全加密反向代理中继架构，打通无公网 IP 工控机与腾讯云 VPS 的双向隧道；配合 Systemd 守护与 WAL 读写分离，实现断电 15s 自愈与 5s 无感主备秒级切换。
- **标准化工程交付与自动化运维**：编写全自动一键部署 Shell 脚本与离线交付包，输出标准化《交付与运维手册》与实机接入 SOP，大幅降低现场交付与联调周期。

---

## 🚀 二、 面试深度版（STAR 结构化拆解与高频问答要点）

### 1. 【Situation 项目背景】
传统制造业现场存在不同品牌（华数臂、珞石 AMR、四足机器狗等）通信协议孤岛、现场多处于无公网 IP 的局域网环境、远程运维困难、工况缺乏可视化与智能化控制等痛点。项目旨在构建一套支持多设备并发、低延迟穿透、实时 3D 姿态展示与自然语言控制的现代化工业物联网云平台。

### 2. 【Task 核心职责与任务】
- 负责端到端全栈研发：从现场工控机代理（Edge Agent）、云端网关 API、MQTT 消息路由、数据库持久化到前端可视化大屏；
- 解决现场机械臂底层 SDK 通信逆解与数据清洗；
- 搭建高可用、断电自愈的云端穿透网络与主备容灾体系。

### 3. 【Action 关键技术攻关与落地】
1. **高并发物联网总线设计**：
   - 规范 `robot/{type}/{id}/state` 与 `robot/{type}/{id}/sensor` 主题矩阵，设计高效 JSON 报文格式，支持秒级高频多设备并发推流；
2. **底层工业二次开发**：
   - 对接华数 `Hsc3Api`，调用 `mot.getJntData`（关节角）、`mot.getLocData`（笛卡尔位姿）、`mot.getJntEData`（伺服电流）、`vm.getProgStatus`，实现毫秒级数据提取；
3. **AI 工业控制闭环**：
   - 提示词工程 + Few-Shot 构建工业指令 Schema，配合严格的 JSON 输出校验与本地规则引擎双重兜底，杜绝大模型幻觉，确保下发给机器人的指令 100% 安全；
4. **网络穿透与高可用工程**：
   - 配置 FRPS 与 FRPC 双向长连接，映射 Web (8000)、MQTT (1883)、Dashboard (18083) 与远程 SSH (2222)；
   - 编写双机热备（主生产机 + 备用容灾机）故障转移策略。

### 4. 【Result 量化成果与交付】
- **性能指标**：系统并发支持数十台设备稳定推流，端到端控制响应延迟稳定在 **<30ms**；
- **稳定性**：断电或网络波动后，Systemd + FRP 实现 **15秒内全自动恢复上线**，零人工介入；
- **交付效率**：通过一键 Shell 脚本与单文件 Agent，现场交付部署时间由传统的 2 天缩短至 **15 分钟**。

---

## 🛠️ 三、 技术关键词提炼 (ATS 简历筛选优化)

`Python 3.10+` `FastAPI` `EMQX / MQTT` `工业物联网 (IIoT)` `华数机器人二次开发` `数字孪生 / 3D 姿态可视化` `FRP 内网穿透` `LLM 工业指令编译` `SQLite WAL 高并发` `Linux / Systemd 守护` `双机热备容灾` `网络安全与 RBAC 鉴权`
